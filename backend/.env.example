# auto = use a configured neural engine when available
# null = force browser-only frontend fallback
# kokoro = require the Kokoro adapter
AI_VOICE_ENGINE=auto

# Absolute path to a locally downloaded, license-verified ONNX model.
AI_VOICE_KOKORO_MODEL=
